# bunfig.toml
[test]
preload = ["./src/test/@pretest.ts"]
timeout = 30000                                                            # 30 seconds
coverageReporter = ["text", "lcov"]
coveragePathIgnorePatterns = ["dist/**", "client/**", "node_modules/**", "src/test/**"]
pathIgnorePatterns = ["client/**"]

[install]
optional = true
dev = true
peer = true
